<% content_for :sub_title do %>
  Search
<% end %>

<div class="w3-section">
  <%= pluralize(@contacts.count, "query") %> found for <b><%= @query %></b>.
</div>
<%= render 'contacts/display_contacts' %>
