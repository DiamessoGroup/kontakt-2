<%= form_with(model: usertest) do |form| %>
	<% if usertest.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(usertest.errors.count, "error") %> prohibited this usertest from being saved:</h2>

			<ul>
				<% usertest.errors.each do |error| %>
					<li><%= error.full_message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= form.label :name %>
		<%= form.text_field :name %>
	</div>

	<div class="field">
		<%= form.label :password %>
		<%= form.password_field :password %>
	</div>

	<div class="field">
		<%= form.label :password_confirmation %>
		<%= form.password_field :password_confirmation %>
	</div>

	<div class="actions">
		<%= form.submit %>
	</div>
<% end %>
